#include <stdio.h>

struct person {
    int shoe_size;
    double height;
    char first_name_initial;
};

enum opal_card_type {
    ADULT, // 0
    CONCESSION, // 1
    STUDENT // 2
};

int main(void) {

    

    return 0;
}