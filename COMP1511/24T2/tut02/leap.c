#include <stdio.h>

int main(void) {
    
    int year;
    printf("Enter a year: ");
    scanf("%d", &year);

    // if leap, print "[year] is a leap year"
    // if not leap, print "[year] is not a leap year"


}