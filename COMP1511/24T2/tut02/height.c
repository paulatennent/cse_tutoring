#include <stdio.h>

int main(void) {

  // scan in the height

  // if height <= 0 then print an error message

  // if height below 100cm, print "Too short to ride"

  // if height below 150cm, print "Ride with a parent"

  // otherwise, print "You can ride alone"

  return 0;
}
